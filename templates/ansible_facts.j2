#!/bin/bash
# {{ ansible_managed }}
cat <<EOF
{
  "version": "{{ k0s_version }}"{% if __stat_controller_k0s_conf.stat.checksum is defined and __stat_controller_k0s_conf.stat.checksum | string | length > 0 %},
  "config_checksum": "{{ __stat_controller_k0s_conf.stat.checksum }}"
{% endif %}
}
EOF
